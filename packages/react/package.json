{
  "name": "@atomar/react",
  "version": "0.1.1-alpha.0",
  "description": "React bindings and utils",
  "author": "Abdulla Bayramov",
  "main": "esm/index.js",
  "types": "esm/index.d.ts",
  "module": "esm",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@atomar/wrapped": "^0.1.1-alpha.0",
    "@types/react": "^17"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf esm && rm -rf src/*.{js,d.ts,map}",
    "compile": "tsc -p tsconfig.build.json",
    "test": "jest"
  }
}
